# Scientific Boundary

Synthetix outputs are synthetic scenario evidence. They are not observations from
sampled humans. Reports must not claim population prevalence, confidence intervals,
causality, or statistical significance.

Comparisons are valid only within a declared protocol and must retain model,
provider, prompt, decoding, retry, and failure provenance. Cloud runs are recorded
and replayable, not perfectly reproducible.

